spring:
  cloud:
    gateway:
      routes:
            - id: admin_route
              uri: lb://renren-fast
              predicates:
                - Path=/api/**
              filters:
#              正则表达式
                - RewritePath=/api/(?<segment>.*),/renren-fast/$\{segment}
